# Issue 35751 TDD note

Temporary working note for the fork-local reproduction of the resumed-compaction Responses WebSocket tool-surface regression.

The production change must remain bounded to `codex-rs/core/src/client.rs` plus regression coverage in `codex-rs/core/src/client_tests.rs`.
